import * as React from 'react';
import {View} from 'react-native';
import {Navigation} from './navigation';

const App = () => {
  return (
    <View style={{flex: 1}}>
      <Navigation />
    </View>
  );
};

export {App};
